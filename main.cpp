#include <iostream>
#include "GameManager.h"
int main() {

    GameManager* gameManager = new GameManager();

/*    gameManager->BeginPlay();

    while(true)
    {
        gameManager->Tick();
    }

    return 0;*/
}
